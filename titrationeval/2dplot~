#!/usr/bin/env python
import matplotlib
matplotlib.use('Agg')
import sys
import pandas as pd
import matplotlib.pyplot as plt
import os

df = []

for x in range(len(sys.argv)-1):
    df.append(pd.read_csv(sys.argv[x+1]))
    df[x]["Molar Excess"]=x

big_df = pd.concat(df)
fig, ax = plt.subplots(1,1)

x = 'Assign F1'
y = 'Position F1'

'''
for i in df[0][x]:
    print("")
    #print(i)
    #print(big_df[x][i])

'''

print(big_df["Molar Excess"].unique())
print(big_df[x].unique())
'''
for i in big_df[x].unique():
        ax.plot(big_df["Molar Excess"].unique(), big_df[i][y]-big_df[0][y])
'''

ax.set(ylabel=y) 
plt.xticks(rotation=90)
plt.xlabel(x)
plt.tight_layout()
plt.savefig("output.svg")
